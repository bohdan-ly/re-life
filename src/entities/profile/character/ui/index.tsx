import React from 'react';

import { CoverImage } from 'shared/ui/components';

import * as model from '../model';

export const Hero = () => {
  return (
    <section>
      <CoverImage />
    </section>
  );
};

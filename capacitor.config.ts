import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'xyz.reliferpg.reliferpg',
  appName: 'ReLifeRPG',
  webDir: 'out',
  bundledWebRuntime: false,
};

export default config;
